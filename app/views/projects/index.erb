<div class="page-header">
  <h1 class="h4"><%= icon('rocket') %> <%= Project.model_name.human.pluralize %></h1>
  <%= link_to t('projects.new_project'), new_site_project_path, data: { turbo_frame: "new_project" }, class: 'btn btn-primary' %>
</div>

<%= turbo_frame_tag(:new_project) %>

<div class="projects-list">
  <%= render @projects %>
</div>
