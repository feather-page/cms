<%= turbo_frame_tag(:content) do %>
  <%= render PageHeaderComponent.new(title: "Edit Project", breadcrumb_items: [["Projects", site_projects_path(current_site)], ["Edit Project"]]) %>
  <%= render "form", project: @project %>
<% end %>
