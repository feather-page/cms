<%= turbo_frame_tag(:content) do %>
  <%= render PageHeaderComponent.new(title: "New Project", breadcrumb_items: [["Projects", site_projects_path(current_site)], ["New Project"]]) %>
  <%= render "form", project: @project %>
<% end %>
