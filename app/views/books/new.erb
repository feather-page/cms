<%= turbo_frame_tag(:content) do %>
  <%= render PageHeaderComponent.new(title: t('books.new_book'), breadcrumb_items: [["Books", site_books_path(current_site)], [t('books.new_book')]]) %>
  <%= render "form", book: @book %>
<% end %>
