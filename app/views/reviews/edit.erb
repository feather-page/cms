<%= turbo_frame_tag :content do %>
  <div class="page-header">
    <h1 class="h4"><%= icon('pencil') %> <%= t('reviews.edit_title', book: @book.title) %></h1>
  </div>
  <%= render "form", post: @post, form_url: site_book_review_path(@book.site, @book), form_method: :patch %>
<% end %>
