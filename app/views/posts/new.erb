<%= turbo_frame_tag(:content) do %>
  <%= render PageHeaderComponent.new(title: "New Post", breadcrumb_items: [["Blogposts", site_posts_path(current_site)], ["New Post"]]) %>
  <%= render "form", post: @post %>
<% end %>
