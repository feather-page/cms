<%= turbo_frame_tag(:content) do %>
  <%= render PageHeaderComponent.new(title: "New Page", breadcrumb_items: [["Pages", site_pages_path(current_site)], ["New Page"]]) %>
  <%= render "form", page: @page %>
<% end %>
