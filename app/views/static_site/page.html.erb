<%= Blocks::Renderer.render(@page.blocks, routes: static_site_routes) %>

<% if @page.tag_list.any? %>
  <div class="page-tags">
    <% @page.tag_list.each do |tag| %>
      <span class="badge badge-outline"><%= tag %></span>
    <% end %>
  </div>
<% end %>

<% if @page.page_type_books? %>
  <%= render StaticSite::BooksListComponent.new(books: @page.site.books.ordered) %>
<% end %>

<% if @page.page_type_projects? %>
  <%= render StaticSite::ProjectsListComponent.new(projects: @page.site.projects.ordered, routes: static_site_routes) %>
<% end %>
